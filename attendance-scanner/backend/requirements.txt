fastapi==0.111.0
uvicorn[standard]==0.30.1
supabase==2.5.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
opencv-python-headless==4.9.0.80
Pillow>=10.4.0
numpy==1.26.4
python-dotenv==1.0.1
# pytesseract is optional — only needed for server-side /scan/frames endpoint
# Browser-based OCR via tesseract.js is used by default (no system install needed)
# pytesseract==0.3.10
